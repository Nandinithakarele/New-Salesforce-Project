import { LightningElement } from 'lwc';

export default class MarginPadding extends LightningElement {
    //margin --> space outside our playground
    //syntax--> slds-m-<place>_<size>
    //place--> left, right, top, bottom, horizontal(equal space left right),
    //vertical (equal space to top bottom), around
    //size---> xxx-small, xx-small, x-small, medium, large, x-large, xx-large
}